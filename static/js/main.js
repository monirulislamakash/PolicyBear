// const container = document.querySelector('.WhoWeHelp');
// const firstSection = document.querySelector('.first-section');
// const secondSection = document.querySelector('.second-section');

// window.addEventListener('scroll', () => {
//     const rect = container.getBoundingClientRect();
//     const viewportHeight = window.innerHeight;

//     if (rect.top <= viewportHeight / 2 && rect.bottom >= viewportHeight / 2) {
//         const scrollProgress = (viewportHeight / 2 - rect.top) / rect.height;

//         if (scrollProgress > 0.5) {
//             firstSection.classList.add('hidden');
//             secondSection.classList.remove('hidden');
//             secondSection.classList.add('visible');
//         } else {
//             firstSection.classList.remove('hidden');
//             secondSection.classList.remove('visible');
//             secondSection.classList.add('hidden');
//         }
//     }
// });
